export type CustomTheme = {
  text: {
    color: {
      primary: string;
      secondary: string;
      info: string;
      warning: string;
    };
  };
  background: {
    color: {
      primary: string;
      secondary: string;
    };
  };
  shadow: {
    color: {
      primary: string;
      secondary: string;
    };
  };
};
